# CLAUDE.md — صفحة باقات مركز رواد المستقبل للأطفال

صفحة واحدة تعرض باقات اشتراكات المركز وتوصل العميل إلى صفحة المنتج الصحيحة في متجر سلة.

**الإصدار الحالي: v3 — «مرشد الباقات»**: محادثة إرشادية تسأل ثلاثة أسئلة (الفترة ← الساعات ← المدة)
ثم ترسل في فقاعة المحادثة اسم الباقة وسعرها وزر «اشترك الآن». الهدف المعتمد: تقليل الحِمل الذهني
(خصوصًا لكبار السن) — سؤال واحد في كل مرة بدل 42 خيارًا دفعة واحدة.

## المبدأ الأساسي

**`data/packages.json` هو مصدر الحقيقة الوحيد** للأسعار والروابط ونصوص التواصل.
`site/` كله **مولَّد وغير مُتتبَّع في Git** (مُدرج في `.gitignore`) — يبنيه الـCI عند كل دفع.
لا تُحرَّر ملفاته يدويًا أبدًا؛ أي تعديل مباشر عليها يُمحى عند إعادة البناء.

## البنية على طبقتين (مهم)

1. **أساس ثابت يعمل بلا JavaScript** — جدول الأسعار الكامل داخل `#rw-fallback`
   (radio inputs مخفية + `:checked`)، وفيه الـ42 رابط منتج في HTML ثابت.
   هذا ما يظهر لمن عطّل JS، ولمن فشل عنده السكربت، وقبل تنفيذه.
2. **تحسين تدريجي** — `src/chat.js` يبني المحادثة في `#rw-chat`، ويضيف الصنف `rw-js`
   على `#rw-baqat` فيُخفي الأساس، ويقرأ الأسعار من `<script type="application/json" id="rw-data">`
   المضمَّن وقت البناء (لا fetch ولا API).

**قاعدة الفشل الآمن**: `rw-js` لا يُضاف إلا بعد نجاح البناء كاملًا. أي خطأ مبكر (بيانات تالفة،
عنصر مفقود) يخرج من السكربت بهدوء ويترك جدول الأسعار ظاهرًا. لا تُغيّر هذا الترتيب.

زر «أعرف ما أريد — أعرض كل الأسعار» يعيد إظهار الأساس نفسه (`.rw-open`) — لا نسخة ثانية من الشبكة.

## بنية الملفات

```
data/packages.json          مصدر الحقيقة: الفترات، الباقات، الأسعار، الـ42 رابط منتج، بيانات التواصل
src/styles.css              CSS تصميم v3 المعتمد — لا يُغيَّر إلا بموافقة تصميمية
src/chat.js                 سكربت المحادثة (vanilla، بلا اعتماديات) — يُضمَّن inline وقت البناء
src/build.mjs               يولّد site/ من packages.json + styles.css + chat.js
src/verify.mjs              فحوصات ما قبل النشر (تفشل بكود خروج 1 عند أي خلل)
src/serve.mjs               خادم ثابت للمعاينة المحلية (بدون اعتماديات)
.github/workflows/deploy.yml  بناء + تحقق + نشر تلقائي عند كل دفع
site/index.html             ← مولَّد (خارج Git): الصفحة الكاملة المنشورة
site/embed.html             ← مولَّد (خارج Git): مقتطف الودجت فقط للصق في محرر HTML داخل سلة
design_handoff_mursheed/    تسليم v3 المعتمد: README المواصفة + mursheed.dc.html النموذج
design_handoff_baqat_v2/    تسليم v2 (مرجع تاريخي)
design_handoff_baqat/       تسليم v1 (مرجع تاريخي)
```

## كيفية التعديل وإعادة البناء

تغيير سعر أو رابط منتج:

1. عدّل القيمة في `data/packages.json` فقط. تصل تلقائيًا إلى الجدول الثابت **وإلى المحادثة**
   (كلاهما مولَّد من الملف نفسه).
2. `npm run check` (بناء + تحقق) ثم `npm run serve` للمعاينة على `http://localhost:4173`.
3. `git push` — والباقي تلقائي.

التعديل من واجهة GitHub مباشرة يعمل أيضًا. `npm run deploy` متاح للنشر اليدوي الفوري.

## ما الذي يفحصه `verify.mjs`

**بيانات**

- كل رابط منتج يظهر مرتين بالضبط (مرة في HTML الثابت ومرة في JSON المحادثة)،
  ورابطا القسمين مرة واحدة، ولا روابط يتيمة.
- **JSON المحادثة مطابق تمامًا لـ`packages.json`** — 42 سعرًا ورابطًا، لأن صحة ما يعرضه
  المرشد تعتمد على هذه الكتلة وحدها.
- كل سعر مقترن برابط منتجه داخل نفس البطاقة في الأساس الثابت.
- **معرّفات `packages.json` = معرّفات نموذج v3 المعتمد** (`design_handoff_mursheed/mursheed.dc.html`)
  في الاتجاهين — مصدر مستقل يكشف أي انحراف في البيانات عن التسليم.

**العمل بلا JavaScript**

- `#rw-fallback` موجود ويحوي الـ44 رابطًا في HTML ثابت.
- لا سكربتات خارجية (`<script src`) — قيد سلة. وكتلتا سكربت فقط: البيانات والمحادثة، كلاهما داخل `#rw-baqat`.
- البناء يرفض أي `</script` أو `<!--` داخل `chat.js` (يكسران تحليل الوسم).

**تصميم v3**

- نصوص المحادثة الحرفية كلها (الترحيب، الأسئلة الثلاثة، رسالتا النتيجة، إعادة البدء، تسمية ما بعدها).
- المقاسات: شعار 60px · عنوان `clamp(23,5vw,32)` · فقاعات `clamp(17,3.6vw,19)`/`clamp(18,3.8vw,21)` ·
  سعر النتيجة 46px · زر النتيجة وأزرار الرد 62px · مجرى المحادثة 520px ·
  بطاقة الجدول: عنوان 26px وسعر 44px وزر 58px · أزرار المدة 54px · التواصل 56px.
- الشبكة 3 أعمدة، ونقطتا التحول 900px و600px، والحركتان `rwIn` و`rwDot`.
- **ممنوع**: `position:sticky` (أُلغيت في v3) · `scrollIntoView` (المواصفة تنص على `scrollTop`) ·
  بقايا v1/v2 (`rw-badge`، «الساعه»، «تغيير الفترة»، `rw-landing`، شبكة 6 أعمدة، نقطتا 1019px و560px).
- الأعداد: 12 بطاقة ثابتة · بطاقة «ساعة واحدة» محصورة في مدة «اليوم».
- لا `min-height` أقل من 44px، ولا لون خارج اللوحة المعتمدة.

> لا توجد مقارنة بايت-ببايت بملف مرجعي: v3 مبني من **مواصفة مكتوبة** لا من ملف يُنسخ.
> حلّت محلها التأكيدات أعلاه، ورسائل فشلها تحدّد أي رقم أو نص انحرف.

## قيود التصميم (معتمدة من العميل — لا تُغيَّر بدون طلب صريح)

- النصوص العربية تُنسخ حرفيًا، لا يُعاد صياغتها — نصوص المحادثة خصوصًا.
- اللوحة اللونية: `#D60859` · `#941249` · `#5E0B30` · `#E8BD4B` · `#2A1B22` · `#4A323E` ·
  `#FBF2E7` · `#F0DECC` · `#EAD9C6` · `#F3D9E4` · `#fff`، وتدرجات الذهبي `#FFF7E8`/`#FFEBC7`/`#FFE9C4`.
- الخطوط: `Baloo Bhaijaan 2` للعناوين والأرقام وفقاعة العميل، `Almarai` للمتن (Google Fonts).
- الشبكة: 3 أعمدة > 900px ← عمودان ≤ 900px ← عمود واحد ≤ 600px.
- كل زر بارتفاع ≥ 44px (الرئيسية 54–62px).
- **JavaScript اختياري**: مسموح، لكن الصفحة يجب أن تبقى صالحة ومكتملة الروابط بدونه.
  (كان القيد في v1/v2 «لا JavaScript إطلاقًا» — تغيّر باعتماد v3.)
- روابط المنتجات تفتح في تبويب جديد (`target="_blank" rel="noopener"`) — تغيّر عن v2.

## قرارات وانحرافات عن نموذج v3 (وسببها)

1. **الشريحتان محذوفتان**: «التقسيط متاح عبر تابي وتمارا» و«اشتراك مرن بدون تجديد تلقائي»
   غير موجودتين في مواصفة v3 ولا في نموذجها، فحُذفتا اتّباعًا للمواصفة.
   `packages.json → notes` صار غير مستخدَم. **إن أردت إعادتهما فهو قرار العميل.**
2. **روابط القسمين مُبقاة** تحت شبكة الأسعار، رغم أن النموذج لا يعرضها — لأن قسم «التحقق»
   في مواصفة v3 ينص صراحة على بقاء «42 رابط منتج + رابطا القسمين».
3. **أزرار الرد `<button>` لا `<div>`**: النموذج يستخدم `div onClick` وهو غير قابل للوصول
   بلوحة المفاتيح. البصريات مطابقة تمامًا.
4. **صف اختيار الفترة داخل الجدول يظهر بلا JS فقط**: في تصميم v3 تحدَّد الفترة من المحادثة،
   فلا وجود لعنصر تحكم بها. لكن بلا JS لا محادثة، فيلزم عنصر لاختيار الفترة.
5. **`@media (prefers-reduced-motion:reduce)` مضاف**: يعطّل `rwIn`/`rwDot`. يحترم تفضيل المستخدم،
   ويمنع بقاء الفقاعات شفافة لو لم تُشغَّل الحركة (`animation-fill-mode:both` من `opacity:0`).
6. **زرّا الفترة بلا سطر وقت**: مواصفة v3 تضع تحتهما «من 6:45 صباحاً» و«حتى 9 مساءً»،
   وحُذفا **بطلب العميل**. الدوام يبقى معروضًا في التذييل. `verify.mjs` يمنع عودتهما.
7. **زر «رجوع ↩» مضاف بطلب العميل** (غير موجود في مواصفة v3): يعيد الخطوة السابقة.
   آليته: `snapshot()` قبل كل اختيار يسجّل `{stage, period, hours, عدد الرسائل}`،
   و`goBack()` **يلغي المؤقتات أولًا** ثم يحذف الرسائل الزائدة ويعيد الحالة.
   إلغاء المؤقتات ليس تفصيلًا: بدونه تصل رسالة مؤجّلة بعد الرجوع فتُفسد المحادثة (مُختبَر).
   الزر مخفي عند السؤال الأول وبعد «ابدأ من جديد» (`history` يُفرَّغ).

## النشر

- **تلقائي**: كل دفع إلى GitHub يشغّل `.github/workflows/deploy.yml` → `npm run check` ثم رفع `site/`
  إلى Cloudflare Pages. **فشل التحقق يوقف النشر**.
  - الدفع إلى `main` ← نشر إنتاجي على `futurepioneers-baqat.pages.dev`.
  - الدفع إلى أي فرع آخر ← رابط معاينة مستقل باسم الفرع.
- **الحسابات**: GitHub `futurepioneers2030/futurepioneers-baqat` (عام) ·
  Cloudflare `futurepioneers2030@gmail.com` · مشروع Pages `futurepioneers-baqat` (رفع مباشر).
- **أسرار المستودع** (Settings → Secrets and variables → Actions):
  `CLOUDFLARE_API_TOKEN` و`CLOUDFLARE_ACCOUNT_ID`. كلاهما مضبوط.
- عند تدوير المفتاح: أنشئ رمزًا جديدًا من لوحة Cloudflare وحدّث السرّ — لا تضع الرمز في أي ملف.
- متابعة التشغيلات: `gh run list` · إعادة تشغيل يدوية: `gh workflow run deploy.yml --ref main`.
  تشغيلان متزامنان على الفرع نفسه؟ يُلغى الأقدم تلقائيًا ويبقى الأحدث.

## ملاحظتان عند المعاينة داخل متصفح غير مرئي

1. لوحة المعاينة المدمجة لا تُنتج إطارات (`requestAnimationFrame` لا يعمل)، فتتجمّد حركات CSS
   وانتقالاته. فقاعات المحادثة تبدأ بـ`opacity:0` عبر `rwIn`، فقد تبدو **غير مرئية** تمامًا.
   عطّل الحركات أولًا (`#rw-baqat *{animation:none !important;transition:none !important}`)
   قبل أي قياس، وإلا ستقرأ نتيجة خاطئة. هذا أثر قياس فقط ولا يمس الصفحة في متصفح حقيقي.
2. لا تُعِد ضبط `innerHTML` لحاوية المحادثة أثناء الاختبار — تُفقد مستمعات الأحداث وتتعطّل الأزرار.
   لإعادة الحالة أعد تحميل الصفحة.
