# CLAUDE.md — صفحة باقات مركز رواد المستقبل للأطفال

موقع ثابت (static) بصفحة واحدة يعرض باقات اشتراكات المركز، وكل زر «اشترك الآن» يفتح صفحة المنتج
الصحيحة مباشرة في متجر سلة. الصفحة **بدون أي JavaScript** — كل التفاعل CSS فقط عبر `radio inputs` مخفية.

## المبدأ الأساسي

**`data/packages.json` هو مصدر الحقيقة الوحيد** للأسعار والروابط ونصوص التواصل.
`site/` كله **مولَّد وغير مُتتبَّع في Git** (مُدرج في `.gitignore`) — يبنيه الـCI عند كل دفع.
لا تُحرَّر ملفاته يدويًا أبدًا؛ أي تعديل مباشر عليها يُمحى عند إعادة البناء.

## بنية الملفات

```
data/packages.json          مصدر الحقيقة: الفترات، الباقات، الأسعار، الـ42 رابط منتج، بيانات التواصل
src/styles.css              CSS التصميم المعتمد (منقول حرفيًا من التصميم المرجعي — لا يُغيَّر إلا بموافقة تصميمية)
src/build.mjs               يولّد site/ من packages.json + styles.css
src/verify.mjs              فحوصات ما قبل النشر (تفشل بكود خروج 1 عند أي خلل)
src/serve.mjs               خادم ثابت للمعاينة المحلية (بدون اعتماديات)
.github/workflows/deploy.yml  بناء + تحقق + نشر تلقائي عند كل دفع
site/index.html             ← مولَّد (خارج Git): الصفحة الكاملة المنشورة
site/embed.html             ← مولَّد (خارج Git): مقتطف الودجت فقط للصق في محرر HTML داخل سلة
design_handoff_baqat/       ملفات التسليم الأصلية (مرجع تاريخي + مرجع المقارنة في verify)
```

## كيفية التعديل وإعادة البناء

تغيير سعر أو رابط منتج:

1. عدّل القيمة في `data/packages.json` فقط.
2. `npm run check` (بناء + تحقق) ثم `npm run serve` للمعاينة على `http://localhost:4173`.
3. `git push` — والباقي تلقائي.

التعديل من واجهة GitHub مباشرة يعمل أيضًا: الـCI يبني ويتحقق وينشر، ولا يوجد مخرَج
مُتتبَّع في Git يمكن أن يتخلّف عن البيانات.

`npm run deploy` يبقى متاحًا للنشر اليدوي الفوري من الجهاز عند الحاجة.

## ما الذي يفحصه `verify.mjs`

- كل رابط في `packages.json` يظهر في الصفحة **مرة واحدة بالضبط** — ولا روابط يتيمة في الصفحة.
- عدد روابط المنتجات = 42 (21 صباحي + 21 مسائي) + رابطا قسم.
- كل سعر مقترن برابط منتجه داخل نفس البطاقة.
- **الودجت المولَّد مطابق للتصميم المرجعي** `design_handoff_baqat/reference-design.html`
  بعد تجاهل المسافات والتعليقات، و**بتقنيع الأسعار والروابط** لأنها بيانات متغيرة بطبيعتها.
  أي فرق بعد التقنيع (لون، مقاس، بنية، نص عربي معتمد) = انحراف تصميمي → أوقف وراجع.
  مُختبَر: تعديل سعر أو رابط في JSON يمرّ؛ تغيير لون في `styles.css` أو نص في `build.mjs` يفشل.
- RTL، الخطان محمّلان، شبكة 6 أعمدة، نقطتا التحول 1019px و600px، أهداف لمس 44px، وعدم وجود `<script>`.

## قيود التصميم (معتمدة من العميل — لا تُغيَّر بدون طلب صريح)

- النصوص العربية تُنسخ حرفيًا، لا يُعاد صياغتها. (بما فيها «الساعه 25 ريال» كما هي.)
- اللوحة اللونية: `#D60859` وردي · `#941249` عناب · `#5E0B30` عناب داكن · `#E8BD4B` ذهبي ·
  `#2A1B22` نص · `#4A323E` نص ثانوي · `#FBF2E7` خلفية · `#F0DECC` و`#EAD9C6` حدود. لا ألوان خارجها.
- الخطوط: `Baloo Bhaijaan 2` للعناوين والأرقام، `Almarai` للمتن (Google Fonts).
- الشبكة: 6 أعمدة > 1019px ← 3 أعمدة ≤ 1019px ← عمودان ≤ 600px.
- كل زر/تبويب بارتفاع ≥ 44px.
- **لا JavaScript** — إن اضطررت لإضافته، يجب أن تبقى الصفحة صالحة بدونه.

## النشر

- **تلقائي**: كل دفع إلى GitHub يشغّل `.github/workflows/deploy.yml` → `npm run check` ثم رفع `site/`
  إلى Cloudflare Pages. **فشل التحقق يوقف النشر**، فلا يصل خطأ في البيانات أو انحراف تصميمي للإنتاج.
  - الدفع إلى `main` ← نشر إنتاجي على `futurepioneers-baqat.pages.dev`.
  - الدفع إلى أي فرع آخر ← رابط معاينة مستقل باسم الفرع (للتطوير قبل الدمج).
- **الحسابات**: GitHub `futurepioneers2030/futurepioneers-baqat` (عام) ·
  Cloudflare `futurepioneers2030@gmail.com` · مشروع Pages `futurepioneers-baqat` (رفع مباشر).
- **أسرار المستودع المطلوبة** (Settings → Secrets and variables → Actions):
  `CLOUDFLARE_API_TOKEN` (صلاحية Cloudflare Pages: Edit) و`CLOUDFLARE_ACCOUNT_ID`.
- عند تدوير المفتاح: أنشئ رمزًا جديدًا من لوحة Cloudflare وحدّث السرّ — لا تضع الرمز في أي ملف.

## ملاحظة عند المعاينة داخل متصفح غير مرئي

لوحة المعاينة المدمجة لا تُنتج إطارات (`requestAnimationFrame` لا يعمل)، فتتجمّد انتقالات CSS
وتُرجِع `getComputedStyle` قيمة اللون قبل الانتقال. عند فحص تمييز التبويب النشط برمجيًا،
عطّل الانتقالات أولًا (`#rw-baqat *{transition:none !important}`) وإلا ستقرأ نتيجة خاطئة.
هذا أثر قياس فقط ولا يمس الصفحة في متصفح حقيقي.
